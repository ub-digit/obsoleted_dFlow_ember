<div class="panel panel-default">
  <div class="panel-heading">
    <div class="panel-title">{{t 'users.new'}}</div>
  </div>
  <div class="panel-body">
    <div class="form-group">
      {{partial 'users/index/userform'}}
      <div class="col-xs-12">
        <button class="btn btn-primary" {{action 'createUser' model}}>{{t 'users.create'}}</button>
        {{#link-to 'users.index' class="btn btn-default"}}{{t 'users.cancel'}}{{/link-to}}
      </div>
    </div>
  </div>
</div>
